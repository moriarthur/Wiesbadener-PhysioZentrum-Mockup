[build]
  publish = "build"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Ensure favicon is accessible
[[redirects]]
  from = "/favicon.ico"
  to = "/favicon.ico"
  status = 200